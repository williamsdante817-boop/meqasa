export interface FormState {
  search: string;
  propertyType: string;
  bedrooms: string;
  minPrice: string;
  maxPrice: string;
  minArea: string;
  maxArea: string;
  period: string;
  sort: string;
  furnished: boolean;
  owner: boolean;
  bedroomRadio: string;
}
